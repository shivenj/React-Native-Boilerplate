// Types
export const types = {
}

const initialState = {
}

// Reducer
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

// Actions
export const actions = {
}
