import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  innerSurface: {
    height: 100,
    width: 100,
    alignSelf: 'center',
    elevation: 1,
    borderRadius: 5,
  },
})

export default styles
